package pizzicato.model.dao;

public class PizzanTayteDAO extends DataAccessObject{
	
	/* kesken!!!!!!!!!!
	 * 
SELECT p.pizza_id, p.nimi, x.tayte_id, t.tayte
FROM pizza p
JOIN pizzantayte x
ON x.pizza_id = p.pizza_id
JOIN tayte t
ON t.tayte_id = x.tayte_id;
	
	 */

}
